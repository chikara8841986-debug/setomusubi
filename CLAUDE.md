# CLAUDE.md - Setomusubi

Read `AGENTS.md` first.

This file exists so Claude Code can discover the project-specific startup rules. The full operating context is centralized in `ai-agent-org`; do not copy it into this repository.

Required startup sequence:

1. Read this folder's `AGENTS.md`.
2. Resolve the central `ai-agent-org` path using the PowerShell snippet in `AGENTS.md`.
3. Read the central Setomusubi current-state note before coding.
4. Read `RUNTIME_STATUS.md` and confirm the live runtime and deployment surfaces before editing.
5. Before making code changes, state the exact files to edit, why they are on the live path, and what validation will prove the change.
